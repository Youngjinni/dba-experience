DBA 인턴십 중 실제로 적용하고 검증한 인덱스 설계 전략을 정리했습니다.

---

## 기본 원칙

> **인덱스는 많을수록 좋지 않다.**  
> SELECT 속도는 빨라지지만, INSERT / UPDATE / DELETE 시 인덱스도 함께 갱신되어 DML 성능이 저하됩니다.

| 인덱스 수 | SELECT | DML |
|-----------|--------|-----|
| 적음 | 느림 | 빠름 |
| 많음 | 빠름 | 느림 |

→ **조회 빈도와 DML 빈도를 함께 고려해 선택적으로 생성**

---

## 1. 복합 인덱스(Composite Index) 컬럼 순서 최적화

### 문제 상황
여러 조건을 사용하는 주식 시세 조회 쿼리에서 인덱스 효율이 낮은 문제.

### 핵심 원칙

복합 인덱스는 **첫 번째 컬럼 기준으로 정렬 후 두 번째 컬럼**이 정렬됨.  
선행 컬럼이 조건절에서 누락되거나 범위 조건(`>`, `<`)으로 사용되면 후행 컬럼 인덱스 활용도가 급격히 저하됨.

### 컬럼 순서 결정 기준

1. **동등 조건(`=`)으로 자주 쓰이는 컬럼** 우선 배치
2. **카디널리티(Cardinality)가 높은 컬럼** 우선 배치 (값의 종류가 많을수록 필터링 효과 큼)
3. **범위 조건(`BETWEEN`, `>=`)** 컬럼은 뒤에 배치

### 실제 적용 사례

```sql
--  비효율: 거래일자(범위조건)가 선행 컬럼
CREATE INDEX IDX_STOCK_DATE_CODE
    ON stock_history (trade_date, stock_code);

--  최적: 동등조건인 종목코드가 선행 컬럼
CREATE INDEX IDX_STOCK_CODE_DATE
    ON stock_history (stock_code, trade_date);

-- 특정 종목의 기간별 히스토리 조회 시 아래 인덱스가 훨씬 빠름을 확인
SELECT *
FROM   stock_history
WHERE  stock_code = '005930'                    -- 동등 조건 (선행 컬럼)
  AND  trade_date BETWEEN DATE '2026-01-01'     -- 범위 조건 (후행 컬럼)
                      AND DATE '2026-03-31';
```

---

## 2. Function-Based Index vs Query Reformulation

### 인덱스 억제(Index Suppression) 발생 상황

```sql
--  TRUNC 함수로 인해 인덱스 사용 불가
SELECT * FROM orders
WHERE  TRUNC(order_date) = TO_DATE('2026-03-01', 'YYYY-MM-DD');
```

### 해결 방법 A — 쿼리 개정 (Query Reformulation)

```sql
--  컬럼은 그대로, 비교 대상을 범위로 변경
SELECT * FROM orders
WHERE  order_date >= TO_DATE('2026-03-01', 'YYYY-MM-DD')
  AND  order_date <  TO_DATE('2026-03-02', 'YYYY-MM-DD');
```

### 해결 방법 B — 함수 기반 인덱스 (Function-Based Index)

쿼리 수정이 어렵거나 함수 형태 조회가 불가피한 경우 사용.

```sql
-- 함수 형태 자체를 인덱스로 생성
CREATE INDEX IDX_ORDERS_DATE_TRUNC
    ON orders (TRUNC(order_date));

-- 이메일 대소문자 무관 검색
CREATE INDEX IDX_MEMBER_EMAIL_UPPER
    ON member (UPPER(email));

-- 쿼리도 동일 함수를 사용해야 인덱스 활용 가능
SELECT * FROM member WHERE UPPER(email) = UPPER(:email);
```

> **선택 기준** : 쿼리 수정 가능하면 A방법 우선.  
> 레거시 시스템 등 쿼리 수정이 불가능할 때 B방법 적용.

---

## 3. 인덱스 모니터링

### 사용되지 않는 인덱스 탐지

```sql
-- 모니터링 시작
ALTER INDEX IDX_STOCK_CODE_DATE MONITORING USAGE;

-- 일정 기간 후 사용 여부 확인
SELECT INDEX_NAME, TABLE_NAME, USED
FROM   V$OBJECT_USAGE
WHERE  TABLE_NAME = 'STOCK_HISTORY';

-- USED = 'NO' → 삭제 검토 대상 (DML 성능 저하 요인 제거)
```

---

## 인덱스 생성 결정 기준

| 조건 | 결정 |
|------|------|
| 조회 빈도 높음 + 선택도 높음 |  생성 |
| DML(INSERT/UPDATE) 빈도 매우 높음 |  신중하게 검토 |
| 선택도 낮음 (예: Y/N 두 값만 존재) |  단독 인덱스 비효율 |
| `LIKE '%값'` 패턴 |  B-Tree 인덱스 효과 없음 |
| 함수 적용 컬럼 조회 빈번 + 쿼리 수정 불가 |  Function-Based Index 검토 |
| 복합 조건 쿼리 |  Composite Index (컬럼 순서 신중히) |
